import 'package:flutter/material.dart';

class RadarChartSFC extends StatelessWidget {
  const RadarChartSFC({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('サポートされていません'));
  }
}
